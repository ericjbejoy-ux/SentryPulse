# Copy to .env and fill in. (.env is gitignored — never commit real keys.)
# Backend
GROQ_API_KEY=
DEMO_FALLBACK_MODE=true
N8N_WEBHOOK_URL=http://localhost:5678/webhook-test/sentrypulse-trigger
# Frontend (Vite). When running `npm run dev` locally, the default below is fine.
VITE_API_URL=http://localhost:8000
